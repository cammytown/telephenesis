#starContextMenu.menu
	.user
		a.nav(href="/bookmark") Bookmark

		a.create.recreateStar(
			href="/recreate"
			class = (user.recreationTickets > 0 ? '' : 'ticketless')
		) Recreate

		a.nav.admin(href="/moveStar") Move
		a.nav.admin(href="/recolorStar") Recolor
		a.nav.admin(href="/renameStar") Change Creator
		a.nav.admin.danger(href="/deleteStar") Delete

	.guest
		a.nav(href="/login") login
		a.nav(href="/register") register

#galaxyContextMenu.menu
	.user
		a.create.createStar(
			href="/create"
			class = (user.creationTickets > 0 ? '' : 'ticketless')
		) Create Constellation

	.guest
		a.nav(href="/login") login
		a.nav(href="/register") register
