
header#galaxy-ui
	nav.site-nav
		a#logo Telephenesis
		a.menuToggle |||

		#menu
			.user
				a.create.nav(href="/settings") profile
				a.nav(href="/logout") logout

			.guest
				a.nav(href="/login") login
				a.nav(href="/register") register

			a.nav(href="/help") help

	#sortingUI
		//-///REVISIT not sure about use of nav in terms of best semantics
		nav.order-nav
			span order: 
			span.current-order galaxy

			ul
				li
					a.sort(data-order="galaxy") galaxy
				li
					a.sort(data-order="most-recent" data-header="Sorting by Most Recent") by date
				li
					a.sort(data-order="most-popular" data-header="Sorting by Most Popular") by popularity
				li
					a.sort(data-order="bookmarks" data-header="Your Bookmarks") bookmarks
				//- li recently played

		nav.view-nav
			span view: 
			span.current-view galaxy

			ul
				li
					a.sort(data-view="list") list
				li
					a.sort(data-view="grid") grid

		header
			h2.view-header Sorting by Most Recent

	header#playingStarInfo.inactive
		div#playbackProgress
			div#playbackProgressBar
			// div#playbackProgressAccent

		h2#playingStarTitle Song Title
		h3#playingCreatorName Artist Name
		a#playingCreatorLink artistwebsite.com
